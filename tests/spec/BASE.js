
define( function (require) {
	'use strict';
	
	var base = require('agj/number/base');
	
	describe("number/base", function () {

		it("test assertion", function () {
			expect(true).toBe(false);
		});

		it("", function () {
			expect( base() ).toBe(  );
		});

	});

});
